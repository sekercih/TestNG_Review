package test.day15;

public class Review {
    //todo
    // * Allerts :basit alert sadece ok basmaya izin verir
    // confirm :(onaylama) 2 özellikli ok ve cancel diyebiliriz
    // propt: kullanıcıdan bilgi almak ve ok ve cancel tuşlarına basmak için kullanılır
    // bir iput girişi var diğeri tamam ve iptal etmek için ayrı ayrı 3 fonksiyon.
    // accept()==> tamam (ok) için kullanılan
    // dismiss()==> iptal (cancel) için kullanılan.
    // sendKeys==> Uyarı yazı girmek için kullanılır.
    // getText()==> Webelementteki Mevcut yazıyı almak için kullanırızç
    // alert için işlem sırasında mutlaka switchTo() methodunu kullanırız.
    // driver.switchTo().alert.accept(); syntax şeklinde kullanımı vardır.
    // Bir websitesinde çalışırken başka bir web sitesine alıntı yapılmasına izin vern taglar vardır
    // bunlar iframe olarak adlandırılır.
    // ifram de işlem yapmak için ilk önce index,id,webelemnt iframe geçiş yapılır.iframe geçiş yapıldıktan sonra drivermız iframin içindedir.
    // iframeden çıkış yapılmadan driver oradan hareket etmez
    // sytaxi  driver.switchTo().defaultContent();   driver.switchTo().parentFrame(); şeklinde çıkış yöntemi vardır.
}
